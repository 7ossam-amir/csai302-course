# Live labs

Live labs are hands-on sessions with discussion embedded throughout: review a problem, implement, test, discuss the observed result, then modify the solution for an edge case.

Each live lab is in its own numbered folder, paired with the corresponding pre-lab. Lab 1 is the initial module; future lab folders are added as they are developed.
