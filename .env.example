# Local teaching environment only. Do not use these credentials for a real service.
POSTGRES_USER=csai302
POSTGRES_PASSWORD=csai302-local-only
POSTGRES_DB=csai302
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
